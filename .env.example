# Host advertised to the ESP32 in OTA JSON (not the bind address).
ADVERTISE_HOST=192.168.50.188

# Device-facing ports — keep these unless you re-provision the board.
OTA_PORT=8002
WS_PORT=8000

# Next.js dashboard
UI_PORT=3000
